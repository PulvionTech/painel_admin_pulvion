/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router está habilitado por padrão a partir do Next.js 14, portanto não é
  // necessário ativar `experimental.appDir`.
  reactStrictMode: true,
};

export default nextConfig;